## 1. Identidad v3 en tokens

- [x] 1.1 Re-mapear en `web/src/styles/tokens.css` los valores v3: `--bg-base #0a0a0b`, superficies `#131319`/`#0f0f12`, `--text-primary #f2f2f4`, secondary `#a1a1ab`, `--accent #5468ff`, `--accent-strong #4353cc`, `--accent-soft rgba(84,104,255,0.18)`, `--accent-contrast #101218`, bordes `#1f1f24`/`#23232b`, `--success #5ce0a8`, `--link #a7b0ff`, `--link-hover #cdd2ff`, `--selection rgba(84,104,255,0.35)`, `--focus-ring #5468ff`
- [x] 1.2 Aplicar severidades v3 tono-por-tono en `tokens.css`: crítico fg `#ff8f80`/borde `rgba(255,90,70,0.38)`/bg `rgba(255,80,60,0.14)`, alto fg `#ffc46b`, medio fg `#a7b0ff`, bajo fg `#a1a1ab` (manteniendo los `--severity-*-soft` vía `color-mix`)
- [x] 1.3 Agregar tokens v3 nuevos en `tokens.css`: `--grid-line rgba(255,255,255,0.016)`, `--bg-radial rgba(84,104,255,0.13)`, `--sidebar-w 272px`
- [x] 1.4 Agregar dependencia `@fontsource/chivo` en `web/package.json` e importar weights 400/500/700 latin en el entry de la web
- [x] 1.5 Poner `--font-ui` en la pila Chivo y conservar `--font-mono` (JetBrains Mono) para telemetría
- [x] 1.6 Sincronizar `web/src/lib/colorTokens.ts` en lockstep (`TOKEN_ACCENT #5468ff`, `TOKEN_ACCENT_STRONG #4353cc`, `TOKEN_BG_ELEVATED` v3)
- [x] 1.7 Re-derivar en `web/src/features/geo-map/colorScale.ts` la rampa `BUCKET_COLORS` (5 buckets azul v3) y `MAP_NEUTRAL_COLOR` desde los nuevos accent/bg-elevated
- [x] 1.8 Implementar el fondo v3 con `body::before` (gradiente radial `rgba(84,104,255,0.13)` + grid overlay 56px `rgba(255,255,255,0.016)`, `pointer-events:none`, estático) en `web/src/styles/app.css`
- [x] 1.9 Verificar Fase 1: `pnpm --dir web test` (incluye `colorScale.test.ts` del drift test), `pnpm --dir web lint`, `pnpm --dir web build`, y auditar contraste AA del login standalone contra el mockup v3

## 2. Componentes v3 por pantalla

- [x] 2.1 Sidebar v3: gradiente `linear-gradient(180deg,#131319,#0c0c10 62%,#090909)`, icono escudo `linear-gradient(150deg,#38449e,#141733)`, marca "HONEYPOT SOC / threat ops", sección "Operación" con item activo (barra 3px + gradiente), panel "Servicios" con estados de `useHealth` (operativo/degradado/caído), "Cerrar sesión", `--sidebar-w 272px`, rail ≤900px conservado
- [x] 2.2 Header v3: kicker mono uppercase de la sección + título (de `SECTION_BY_PATH`), dot verde pulsante "24 H" a la derecha (pulse envuelto en `prefers-reduced-motion`), botón "Exportar CSV" existente en el Resumen
- [x] 2.3 Login v3: card `linear-gradient(165deg,#14141b,#0c0c10 62%)`, heading "HONEYPOT SOC / threat ops", indicador "API en línea · v3.0" con pulse, campos Usuario/Contraseña existentes, botón "Ingresar"
- [x] 2.4 Banner "Amenaza activa · severidad crítica" en `AtaquesEnVivo` sobre el evento real de severidad máxima del feed (IP/honeypot/técnica/timestamp, sin inventar campos), con scan line respetando `prefers-reduced-motion` y botón "Investigar" que abre el `EventDetailDrawer`
- [x] 2.5 Chips del feed: estado de conexión (SSE/polling/offline), eventos/s, "en ventana", y botón Pausar/Reanudar (UI-only: congela buffer y ventana EPS, mantiene suscripción SSE)
- [x] 2.6 Drawer de incidente v3: donut SVG de risk score con label central mono + `SeverityBadge`, grid de detalles (IP, geo/ASN, honeypot, protocolo, usuario, password, técnica, sesión, hash, detectado)
- [x] 2.7 Pipeline de automatización n8n en el drawer: timeline vertical de nodos derivado de `responses` (`action_type` como nombre, estado ok/pendiente/reintento según `status`, timestamp, `details` mono), con empty state conservado
- [x] 2.8 Bloques "Comandos ejecutados en la sesión" y `raw_data` re-estilizados (surface/borde/padding v3, mono, `overflow-x:auto`) en el drawer
- [x] 2.9 Explorador/EventsTable, MITRE, mapa (colorScale re-derivado en 1.7), Malware/IoC, Automatización, Workflows n8n y 404 re-estilizados a la identidad v3 (sin residuos cyan)
- [x] 2.10 Verificar Fase 2: `pnpm --dir web test`, `pnpm --dir web lint`, `pnpm --dir web build`, y recorrido manual de las 10 pantallas contra `Honeypot SOC v3.dc.html`

## 3. Resumen bento

- [x] 3.1 Reestructurar `web/src/features/overview/Resumen.tsx` a bento grid CSS (4 columnas, gap ~12px, tiles con spans por clase, reflujo 2→1 columna responsive)
- [x] 3.2 Tile hero (span 2×2): "Total de ataques · 24 h" con `overview.total_eventos` formateado con `Intl.NumberFormat('es-AR')` y sub-stats reales (`ips_unicas`, `total_malware`, count de `alertas_criticas`)
- [x] 3.3 Tile "Eventos por honeypot": barras de progreso con `eventos_por_honeypot` y montos alineados (contenedor con `max-width`)
- [x] 3.4 Tiles "MTTD"/"MTTR" 1×1: `mttd_seconds`/`mttr_seconds` formateados en mono con hint de descripción
- [x] 3.5 Fila inferior: "Top IPs atacantes" (columnas IP/count/risk existentes) y "Alertas críticas" clickeable a detalle (list de `alertas_criticas`)
- [x] 3.6 Conservar loading/error/empty states existentes dentro de los tiles de la bento grid
- [x] 3.7 Verificar Fase 3: `pnpm --dir web test`, `pnpm --dir web lint`, `pnpm --dir web build`, comparación visual del layout contra `Opcion B - Bento.dc.html` y del color contra `Honeypot SOC v3.dc.html`

## 4. Verificación y barrido

- [x] 4.1 Barrido final `rg '06b6d4|0891b2|111827|243244'` en `web/src` sin resultados (sin residuos del look cyan anterior)
- [x] 4.2 Auditoría de contraste AA (≥4.5:1) sobre superficies near-black de primary/secondary/faint nuevos; ajustar faint no esencial o subirlo a `#8b8b95` si falla
- [x] 4.3 Auditoría `prefers-reduced-motion` (desactivar scan/pulse/row-flash) y foco visible 2px `#5468ff` en toda la app
- [x] 4.4 Checklist final contra mockups: identidad v3 en las 10 pantallas (Resumen, Ataques en Vivo, Explorador, MITRE, Mapa, Malware/IoC, Automatización, Workflows n8n, Login, 404), `openspec verify` del change, y confirmación apply-ready con `openspec status --change "rediseno-honeypot-soc"`

## 5. Fase 5 — Ajustes gate visual (feedback operador)

- [x] 5.1 Login: mover el indicador "API en línea · v3.0" al pie del formulario (último elemento del `<form>`, centrado), sin el "pill" (`border` + `padding` + `border-radius 999px`) — línea mono muted `--text-faint`, dot con pulse; variante offline en `--severity-critical`
- [x] 5.2 "Cerrar sesión" rojo sutil: reposo en `color-mix(severity-critical 62%, text-secondary)` (sin el rojo pleno de fatiga de pantalla), hover con `--severity-critical` + `--severity-critical-soft` + borde `color-mix(severity-critical 40%, transparent)`, focus ring global conservado
- [x] 5.3 Resumen bento vs mockup: kicker hero "Total de ataques · 24 H", labels de sub-stats `IPs únicas`/`muestras`/`críticas` con el conteo crítico en `--severity-critical`, hints MTT `detección media`/`respuesta media` + valores en `--severity-medium`, Top IPs como filas con barra proporcional (IP mono + count + riesgo por `riskTone` >=75 crítico / >=50 alto / >=30 medio / resto bajo), Alertas críticas como tabla v3 (Hora/IP/Honeypot/Técnica/Severidad/Riesgo) con filas clickeables accesibles (`role="button"`, `tabIndex=0`, Enter/Espacio, `:focus-visible` accent), "Exportar CSV" del Header en `btn--tint` (gradiente accent 0.18→0.06, borde accent 40%, hover glow `0 8px 26px rgba(84,104,255,0.28)`)
- [x] 5.4 Mapa: hover de países en `--accent`, estado `is-selected` al hacer click en un path (selección toggle con `useState`), panel "Países de origen" convertido de tabla a lista de filas con barra + contador mono, seleccionables y sincronizadas con el mapa (`aria-pressed`), legend mono conservada
- [x] 5.5 Efectos de botones: auditoría de todos los interactivos — filas de alertas (`transition` + `:active` `--bg-hover`), `action-notice-dismiss` (hover con `--success-soft` + `:active`), `page-size-select:hover` borde accent, `btn--tint` añadido al sistema de `--duration-fast`, `box-shadow` integrado a la transición de `.btn`, y neutralización de `transform`/transiciones en `prefers-reduced-motion`
- [x] 5.6 Sidebar colapsable: click en la marca HONEYPOT SOC togglea expanded/collapsed (`useState` + helpers puros testeados `toggleSidebarMode`/`sidebarToggleLabel`/`sidebarRailClass` en `sidebarState.ts`), marca como `<button>` con `aria-expanded` + `aria-label`/`title`, raíl `--sidebar-width-rail` (64px) vía `.app-shell:has(.sidebar--collapsed)`, reglas de raíl replicadas para la clase (y conservadas en el media query ≤900px), labels ocultos con técnica "visually hidden" y servicios degradados a dots con tooltip en el `title` de la fila

## 6. Fase 6 — Unificación de botones tint + sidebar fluido (feedback operador)

- [x] 6.1 Botones unificados a la familia tint: `.btn--primary` pasa a SER el look tint (gradiente `--accent-soft` 0.18→0.06 + borde accent 40% + glow hover `0 0 0 1px accent, 0 8px 26px rgba(84,104,255,0.28)`), alias `.btn--tint` migrado a `.btn--primary` en el Header (Exportar CSV) y eliminado del CSS (single source of truth); CTAs pasadas a primarias: "Exportar CSV" del Explorador, "Ver detalle" (EventsTable), "Bloquear IP" y "Crear ticket GLPI" (Automatización); secundarias (ghost: Cancelar, Restablecer, Limpiar, Cerrar, Pausar/Reanudar, paginación, Reintentar, "Abrir en editor") conservan base neutra con el glow tint sutil al hover `0 0 18px rgba(84,104,255,0.14)`; no se tocan badges/chips/pills/links de navegación/`mitre-row-btn`/`country-row`/`action-notice-dismiss`
- [x] 6.2 "Cerrar sesión" en `.btn--tint-danger`: misma familia tint (gradiente, borde, glow, transición) en la paleta `--severity-critical`; texto en reposo `color-mix(critical 62%, text-secondary)` (~5.4:1 AA, rojo sutil del feedback previo) y critical pleno al hover (~8.6:1 AA), borde `--severity-critical-border`; si el operador prefiere el logout exactamente azul como Exportar, es un cambio de una línea a `.btn--primary`
- [x] 6.3 Sidebar fluido: transición nativa `width` en `.sidebar` + `grid-template-columns` en `.app-shell` (fallback recomendado, ambos animables, 200ms `--duration-base` ease, bidireccional porque la transición vive en el estado base) en lugar de `@property` (evita duplicar 272px en `initial-value` y el riesgo de invalidación global si no se soporta); labels con `max-width`+`opacity`+`overflow:hidden`+`white-space:nowrap` para evitar wrap/clip mid-transition con fade rápido `--duration-fast` (150ms); `prefers-reduced-motion` resetea duraciones a 0.01ms en `.sidebar`/`.app-shell`/labels (salto instantáneo) — verificación estructural (browser no disponible)

## 7. Fase 7 — Card hero mockup (Resumen + Live) (feedback operador)

- [x] 7.1 API overview trend+bloqueos+pytest: `eventos_por_hora` (24 buckets UTC por `date_trunc('hour')` desde honeypot_events, alineados al `to`/ahora) y `bloqueos_ufw` (count de `responses.action_type='bloqueo'`, el valor que escribe el workflow firewall-block) agregados a `schemas/overview.py` (HourlyBucket + defaults opcionales) + `repositories/overview.py` (`_eventos_por_hora`, `_bloqueos_ufw`) + 4 pytest en `test_overview_repo.py` (RED 4 fail → GREEN 13 pass; suite 169→173)
- [x] 7.2 Card Resumen: `BentoHero` reimplementada como `hero-card` fiel al mockup (gradiente 115deg `#141733`→`#0d0d12`, borde `#1f1f24`, radius 8px, fadeUp, scan line accent 7s, label mono "Ataques capturados" `--hero-label`, número Space Mono 700 clamp→56px, sub-stats `IPs únicas`/`muestras`, sparkline 240×48 con `buildTrendGeometry` sobre `eventos_por_hora` REAL, labels −24 H/AHORA, grupo segmentado de tiles Críticas/MTTD/MTTR/Bloqueos tono-por-tono); count-up rAF `useCountUpProgress` + `countUpTargets` (reduced-motion salta a final; MTTD/MTTR "—" si null)
- [x] 7.3 Card Live threat: `AmenazaActiva` re-estilizada a la familia hero-card (gradiente, scan line del tono de severidad, label mono severidad, IP como hero-value, sub-stats reales IP/honeypot/técnica/timestamp, botón Investigar) + fila de stats mono REAL del feed (ev/s, eventos en ventana, fuente SSE/polling/offline); se eliminó el CSS muerto de `.threat-panel`
- [x] 7.4 Font+tokens+CSS: `@fontsource/space-mono` (400/700 latin) en package.json + imports en main.tsx; tokens `--font-mono-hero`, `--hero-bg-start/end`, `--hero-border`, `--hero-label`, `--hero-muted` (mockup `#6e6e78` 3.85:1 < AA → `#8e8e99`), `--hero-scan`, `--trend-fill`, `--hero-tile-bg`; clases `.hero-card`/`.hero-inner`/`.hero-main`/`.hero-label`/`.hero-value`/`.hero-trend*`/`.hero-stats-segmented`/`.hero-stat-tile*` en app.css con anims `fadeUp`/`hero-scan` envueltas en `prefers-reduced-motion` y responsive flex-wrap (1440/900/768/rail)
- [x] 7.5 Gates: `pnpm --dir web test` (152→162), `pnpm --dir web lint` (0 errores, solo warning pre-existente AuthContext), `pnpm --dir web build` (0, subset latin space-mono); backend `powershell -ExecutionPolicy Bypass -File api\run-tests.ps1` (169→174, +4 repo +1 serialización API); `web/src/lib/api.ts` Overview + `HourlyBucket`/`eventos_por_hora`/`bloqueos_ufw`; `tests/conftest.py` insert_response acepta timestamp string (alineado con insert_event)

## 8. Fase 8 — Fusión hero + card Amenaza activa mockup (feedback operador #2)

- [x] 8.1 Fusión hero + eventos por honeypot en `Resumen.tsx`: hero a `grid-column: 1 / -1` full-width (primera fila) con el strip `HoneypotStrip` integrado al pie (filas horizontales compactas: label mono + barra proporcional + count, sobre `eventos_por_honeypot` REAL vía `buildHoneypotStrip`; se oculta completo si vacía, sin empty state); compactación del card (padding interno 24→20px, gap 32→24px, altura sin aumento); tile `.bento-honeypots` eliminado del grid (queda `.bento-topips`/`.bento-alerts` como fila 1×2 sin huecos) y empty/estado muerto limpiado
- [x] 8.2 Card "Amenaza activa" en `AtaquesEnVivo.tsx` = mockup exacto del operador: gradiente `110deg rgba(255,80,60,0.14)→#0d0d12 62%`, border `rgba(255,90,70,0.35)`, radius 8px, scanline 22% con `--scan-duration: 5s`, label mono Space Mono `Amenaza activa · severidad {REAL}`, fila IP 22px 700/mono honeypot 12px `--text-secondary`/técnica mono 12px `--link`/timestamp mono 12px `--hero-muted` (D10) + spacer + botón "Investigar" del mockup (hover glow box-shadow, fuera del kill-list reduced-motion); datos REALES (ip/source_honeypot/att_ck_technique verbatim/timestamp) y handler de Investigar conservado → drawer; se eliminó la fila de stats extra (ev/s, ventana, fuente) y las clases `.threat-card-body/side/stats/action`
- [x] 8.3 CSS/tokens: token `--scan-duration: 7s` (threat-card lo sobreescribe a 5s); paleta threat por tono vía custom properties (critical/unknown roja #ff8f80/#ff6a55/#ffb3a8/#ff6a55, alta `--severity-high` ámbar, media `--severity-medium` azul, baja `#8b93ff`/`#b4baff`); clases `.honeypot-strip*` (title mono `--hero-label` + track pill 6px accent); CSS muerto eliminado: `.bento-honeypots`, `.honeypot-bars`, `.bar-list`, `.bar-row-label`, `.bar-row-count`, `.bar-fill.secondary`
- [x] 8.4 Helpers puros TDD (RED→GREEN): `honeypotStrip.ts` → `buildHoneypotStrip(entries)` donde `pct = Math.round(count/max*100)` (max=1 vacío; tests: []→[], single máx→100%, rounding 67, proporciones variadas, ties, labels/counts preservados) — 7 tests; `threatSeverity.ts` → `threatSeverityTone` (severidad→tono, undefined→unknown) + `THREAT_TONE_LABEL` (suffix, unknown sin sufijo) — 3 tests
- [x] 8.5 Gates: `pnpm --dir web test` (162→172, +10), `pnpm --dir web lint` (0 errores, warning pre-existente AuthContext), `pnpm --dir web build` (0); a11y: tonos nuevos verificados AA sobre #0d0d12/#141733 (único swap #6e6e78→#8e8e99 por D10), scan/fadeUp/count-up en `prefers-reduced-motion`, barras honeypot con texto (no color-solo); responsive hero+strip y threat-card en 1440/900/768 y sidebar colapsado (wrap del strip y de la fila de la card)

## 9. Fase 9 — Tiles horizontales + botones estándar (feedback operador #3)

- [x] 9.1 Tiles del hero en UNA fila horizontal y card más compacta (`app.css`, sin tocar `heroTrend.ts`/`useCountUpProgress`): composición 3 filas — (A) `hero-main` + `hero-trend-block` (trend a la derecha con `margin-left:auto`), (B) `.hero-stats-segmented` a ancho completo propio (`flex:1 1 100%`) con `grid-template-columns: repeat(4, minmax(0,1fr))` y tiles `min-width:0` → nunca 2×2 en 1440/900/768 ni sidebar colapsado, tiles flex-grow para llenar en vez de envolver, (C) `honeypot-strip` intacto; compactación adicional: padding interno 20→16px, gap 24→20px, value `clamp(2.4rem,4vw,56px)`→`clamp(2rem,3.4vw,48px)` — hero 383px→263–280px medido en Chromium real (playwright-core + proxy local :8000→:80)
- [x] 9.2 Pausar/Reanudar ya estaba en `.btn .btn--ghost` (normalizado en Fase 6; la auditoría Fase 9 lo verifica — label dinámico y `aria-pressed` intactos, sin cambios)
- [x] 9.3 "Investigar" de la Amenaza activa migra de `.threat-card-btn` (botón rojo mockup) a `.btn .btn--primary` (familia tint canónica de Fase 6): mismo texto, mismo onClick→`EventDetailDrawer`, sin rutas inventadas; la CARD roja del mockup queda intacta; CSS muerto eliminado: `.threat-card-btn`/`.threat-card-btn:hover` y los custom props `--threat-btn-*` (borde/bg/color/glow) de `.threat-card` y sus tone variants (verificado sin referencias restantes)
- [x] 9.4 Auditoría de botones de la página live: únicos `<button>` reales = sidebar-brand (brand toggle, no acción), `Cerrar sesión` (`.btn--tint-danger`, familia estándar), `Pausar` (`.btn--ghost`) e `Investigar` (`.btn--primary`); banners offline/polling son divs informativos sin botón (reintento automático, no acción manual) — cero botones no estándar que normalizar
- [x] 9.5 Gates Fase 9: `pnpm --dir web test` (172→172), `pnpm --dir web lint` (0 errores, warning pre-existente AuthContext), `pnpm --dir web build` (0); validación visual con Chromium real en 1440/900/768 + sidebar colapsado (tiles siempre 1 fila, card 383→263–280px); a11y sin regresión (labels de tile siguen siendo texto mono, no color-solo; hover/transición de `.btn` ya respetan las reglas de la app)
## 1. Fase 1 — Quick wins (bajo riesgo, mecánico)

- [x] 1.1 `prefers-reduced-motion`: envolver row-flash/pulse/spin de app.css (líneas ~669-680) en `@media (prefers-reduced-motion: reduce)` para desactivarlos. Verificar: activar "reducir movimiento" en devtools → animaciones ausentes sin romper estados; `pnpm --dir web test` verde. _(checkpoint manual devtools pendiente: 3.7)_
- [x] 1.2 Unificar padding vertical de `th`/`td` en app.css (líneas 496/508) a un mismo valor (propuesto 7px 10px). Verificar: tabla del Explorador sin quiebre visual; tests verdes.
- [x] 1.3 Estados de interacción: añadir `:active` + `transition` a `.btn`, `.sidebar-link` y `.mitre-row-btn` en app.css (mantener `:focus-visible` existente). Verificar: feedback táctil al pulsar en las 3 clases; `pnpm --dir web test` verde.
- [x] 1.4 Hook compartido `useDialogLock` en `web/src/hooks/useDialogLock.ts`: scroll-lock (`body { overflow: hidden }`), cierre con Escape y restauración de foco al abridor; aplicarlo en Modal.tsx y EventDetailDrawer.tsx. Opcional: test unitario vitest/jsdom del trap de foco (lista de elementos enfocables). Verificar: abrir modal/drawer → fondo sin scroll, Escape cierra y devuelve foco; tests verdes. _(test jsdom omitido: vitest corre en environment `node` y no hay jsdom/happy-dom instalado; cobertura manual en 3.7)_
- [x] 1.5 Landmark semántico: AppShell.tsx:11 `<div className="app-main">` → `<main className="app-main">`. Verificar: cero impacto visual (el CSS ya apunta a `.app-main`), DOM con `<main>`; `pnpm --dir web test` verde.
- [x] 1.6 Zebra striping: `.data-table tbody tr:nth-child(even)` con tono sutil (token de fondo). Verificar: filas alternas distinguibles en Explorador; tests verdes. _(token `--bg-subtle` añadido a tokens.css)_
- [x] 1.7 Focus-ring en inputs/selects/textarea: reemplazar `outline: none` + borde 1px por `box-shadow: 0 0 0 2px var(--accent-soft)` (app.css líneas ~338/729/1335). Verificar: Tab sobre controles de Login/filtros/modal muestra anillo de 2px; tests verdes.
- [x] 1.8 Eliminar dependencia muerta `recharts@^3.1.0` de web/package.json (verificado: 0 imports en `web/src`; solo lockfile). Ejecutar `pnpm --dir web install`/lock refresh. Verificar: `pnpm --dir web build` compila y grep de `recharts` en `web/src` = 0.
- [x] 1.9 Corregir selector pegado al comentario en app.css:238 y añadir `max-width: 1440px` a `.screen`. Verificar: `pnpm --dir web build` verde; pantalla centrada y ≤1440px en viewport ultrawide. _(checkpoint ultrawide pendiente: 3.7)_

## 2. Fase 2 — Estructural (refactor token-driven)

- [x] 2.1 Expandir tokens.css: `--space-{1..8}` (múltiplos de 4px), `--text-{xs,sm,base,lg,xl,2xl}` (propuesta 12/13/14/16/20/24px), `--z-header:10`, `--z-drawer:50`, `--z-modal:60`, `--duration-{fast,base,slow}` (150/200/300ms), `--radius-lg:12px`, `--focus-ring` (0 0 0 2px var(--accent-soft)) y `--severity-*-soft` vía `color-mix(in srgb, var(--severity-*), 12%, transparent)`. Verificar: tokens declarados y documentados; `pnpm --dir web test` verde. _(también añadidos: `--severity-*-border` (40%), `--success-border`, `--overlay-bg`, `--bg-subtle`, `--bg-hover`; `--text-faint` ya en `#94a3b8`)_
- [x] 2.2 Sweep de espaciado/tipografía: reemplazar los ~57 font-size sueltos y ~41 paddings/margins de app.css por `--text-*` y `--space-*`. Verificar: grep de valores mágicos de tamaño residual ≈0; `pnpm --dir web build`; checkpoint visual de las 8 pantallas. _(grep font-size px = 0; z-index literales = 0; quedan literales geométricos intencionales: 8/9px dots, grosores de borde, line-height, animaciones 0.8/1.6/2s, sombras de drawer/modal)_
- [x] 2.3 Sweep de color: reemplazar los ~26 rgba/hex repetidos (fondos soft de severidad .12/.14/.16/.4, bordes, superficies) por tokens, unificando opacidades divergentes. Verificar: grep de rgba repetidos residual ≈0; tests verdes; severidad visual sin cambio. _(23 rgba de app.css → tokens; atípicas conservadas: sombras drawer/modal rgba(0,0,0,...))_
- [x] 2.4 Primitiva `.btn` con variants `--primary/--ghost/--danger` que absorbe `.btn-accent`, `.login-submit`, `.error-state-retry`; actualizar usos en JSX (Login.tsx, ErrorState.tsx, pantallas). Verificar: grep de clases viejas = 0; `pnpm --dir web test`; checkpoint visual de login y estados de error. _(decisión: `error-state-retry` → `.btn--ghost` + margen propio; pierde el borde accent persistente — desviación anotada)_
- [x] 2.5 Primitivas `.field` (label+control) y `.label`: absorben las recetas de `.login-field input`, `.filter-field input/select`, `.page-size-select`, `.modal-field input/select/textarea` y ~7 labels (10-12px, uppercase, 600, letter-spacing). Verificar: grep de selectores viejos = 0; tests verdes; checkpoint visual de Login/filtros/modal. _(JSX actualizado: Login, EventFiltersBar, MalwareIoc, ActionModals, Modal, paginación, drawer dt)_
- [x] 2.6 Primitiva `.badge` con modifiers `--severity-*`, `--status`, `--action`: absorbe `.badge-severity`, `.badge-status`, `.badge-action`; SeverityBadge.tsx pasa a `.badge--severity-*`. Verificar: grep de clases viejas = 0; tests verdes; checkpoint visual de badges en Resumen/Live/Explorador. _(JSX actualizado: SeverityBadge, Automatizacion, MalwareIoc, WorkflowsN8n)_
- [x] 2.7 Primitivas `.overlay` y `.error-box`: absorben `.drawer-overlay`/`.modal-overlay` (rgba .6/.65) y `.login-error`/`.modal-error`. Verificar: grep de clases viejas = 0; tests verdes; checkpoint visual de modales/drawer/login. _(sobre `.overlay` default (z-drawer) + modifier `.overlay--modal` (z-modal, centrado, padding —- proximidad 48px))_
- [x] 2.8 Severity soft token-driven: los fondos/bordes soft de severidad se resuelven desde la variable `--severity-*` base (D1/D2.1). Verificar: editar `--severity-critical` → badge/fondos críticos soft cambian sin tocar otra regla. _(verificación manual con edit de token pendiente en 3.7)_
- [x] 2.9 Unificar `colorScale.ts` (web/src/features/geo-map/): módulo de constantes de color que derive BUCKET_COLORS/MAP_NEUTRAL_COLOR de los mismos valores de tokens.css; test unitario en colorScale.test.ts que lea tokens.css y falle si las constantes TS se desalinean de la fuente de verdad. Verificar: `pnpm --dir web test` incluye el test de sync; colores del mapa idénticos visualmente. _(TDD: RED (módulo ausente) → GREEN → TRIANGULATE (extremoscante oscuro/claro ≠ tokens); suite 127→129)_
- [x] 2.10 Tokens de z-index aplicados: sustituir los z-index mágicos (10/50/60) fuera de tokens.css por `var(--z-header/--z-drawer/--z-modal)`. Verificar: grep de z-index literales fuera de tokens.css ≈0; overlays siguen por encima de header/sidebar. _(grep = 0)_

## 3. Fase 3 — Pulido fino (responsive + a11y)

- [x] 3.1 Sidebar responsive: rail de iconos a ≤900px (media query: width 64px, labels ocultos, links conservan `title`/`aria-label`, patrón icon-only existente). Verificar: resize ≤900px → rail navegable y accesible, `pnpm --dir web test` verde. _(token `--sidebar-width-rail: 64px`; label envuelto en `<span class="sidebar-link-label">` volteado a visually-hidden + `title` en NavLink; `.sidebar-brand-name/tagline/sidebar-services-title: display:none`)_
- [x] 3.2 Layout responsive restante: padding de `.screen` en @media y preservar @media del mapa (app.css:1080), en coordinación con el max-width de 1.9. Verificar: revisión visual en 768/900/1440/ultrawide. _(media 900px: `.screen { padding: var(--space-4) }`; @media del mapa a 1100px intacta; checklist viewport pendiente 3.7)_
- [x] 3.3 Contraste AA de `--text-faint` (tokens.css:35): subir a `#94a3b8` o reservar el valor actual a textos ≥12px (decisión D8). Verificar: ratio ≥4.5:1 medido sobre su fondo; tests verdes. _(aplicado en 2.1: `--text-faint: #94a3b8`, ratio ~7:1 sobre --bg-surface; confirmación devtools pendiente 3.7)_
- [x] 3.4 Operabilidad por teclado de las filas del Explorador en EventsTable.tsx:42-46: `tabIndex={0}` + `role` + `onKeyDown` Enter/Espacio + `:focus-visible`, o botón/tarjeta "ver detalle" según decisión D4 confirmada. Verificar: Tab recorre filas, Enter/Espacio abre detalle, foco visible; `pnpm --dir web test` verde. _(decisión D4: botón real "Ver detalle" por fila; columna ≤ Acciones con th visually-hidden; se elimina `onClick` de fila y helper `selectionHasText` ya sin uso de producción)_
- [x] 3.5 Focus-ring 2px consistente en todos los interactivos (botones, sidebar-links, filas de tabla, `.mitre-row-btn`). Verificar: recorrido completo con Tab por la app muestra el mismo anillo de 2px. _(regla global `button/a:focus-visible { outline: 2px solid var(--accent) }` cubre .btn/.sidebar-link(a)/.mitre-row-btn/.row-detail-btn; inputs/selects usan `var(--focus-ring)` 2px idéntico; recorrido Tab manual pendiente 3.7)_
- [x] 3.6 Sidebar.tsx en español: 'ok'/'down' → 'operativo'/'caído'. Verificar: UI del sidebar íntegramente en español; `pnpm --dir web test` verde. _(helper `statusLabel()` mapea ok/down/degraded → operativo/caído/degradado en `title` de los dots; clases CSS `.ok/.down` intactas para el color)_
- [ ] 3.7 Gate de cierre integral: `pnpm --dir web test`, `pnpm --dir web lint`, `pnpm --dir web build` todos verdes y checklist visual manual de las 8 pantallas (Resumen, Live, Explorador, MITRE, Mapa, Malware/IoC, Automatización, Login) + checklist a11y (Tab, Escape, reduce-motion en devtools). Verificar: todo en verde y sin regresiones visuales detectadas en el checklist. _(gate automatizado 🌕: 129 tests, lint exit 0, build verde; checklist visual/a11y manual pendiente de ejecución por el operador)_
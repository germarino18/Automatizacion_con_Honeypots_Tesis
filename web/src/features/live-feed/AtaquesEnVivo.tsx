import ScreenPlaceholder from '../../components/ScreenPlaceholder';

export default function AtaquesEnVivo() {
  return (
    <ScreenPlaceholder
      title="Ataques en Vivo"
      description="Feed de eventos por SSE con indicador de actividad en tiempo real."
    />
  );
}

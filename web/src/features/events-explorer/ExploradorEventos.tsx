import ScreenPlaceholder from '../../components/ScreenPlaceholder';

export default function ExploradorEventos() {
  return (
    <ScreenPlaceholder
      title="Explorador de Eventos"
      description="Filtros combinables, paginación, detalle y exportación CSV."
    />
  );
}
